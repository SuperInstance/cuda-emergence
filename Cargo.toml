[package]
name = "cuda-emergence"
version = "0.1.0"
edition = "2021"
description = "Emergence detection — identify when fleet-wide patterns arise that no individual agent was programmed to produce"

[dependencies]
serde = { version = "1", features = ["derive"] }
